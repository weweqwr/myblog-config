spring:
  profiles:
    active: dev
---
spring:
  profiles:
    active: dev
  application:
    name: myblog-dashboard #对外暴露的微服务的名字
server:
  port: 7788
#eureka配置
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://129.204.122.20:7001/eureka/,http://121.36.48.89:7002/eureka/
  instance:
    instance-id:myblog-dashboard-7788 #修改eureka后面url的名字
    prefer-ip-address: true #设置可以显示ip地址
info:
  app:
    name: myblog-dashboard-7788
  company:
    name: www.longtimenosee.top
  build:
    artifactId: $project.artifactId$
    version: $project.version$